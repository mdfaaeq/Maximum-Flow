package solver;

import graph.Edge;
import graph.Graph;

public class MaxFlowSolver {

    public static int fordFulkerson(Graph graph, int startNode, int endNode){
        int maxFlow = 0;

        System.out.println("Starting to find max flow...");

        while(true){
            Edge[] pathEdges = new Edge[graph.num]
        }
    }
}
